# ARMSX2 mobile GameDB overlay.
#
# Applied on tiler-GPU tiers (Android, iOS and ARM64 Linux) on top of
# bin/resources/GameIndex.yaml, through the GameDatabase override path.
#
# gsHWFixes is always replaced wholesale. gameFixes, speedHacks, patches, memcardFilters
# and dynaPatches are replaced wholesale whenever the entry lists at least one item. So
# an entry that names one of those fields must restate every upstream key it wants to
# keep. clampModes and roundModes merge one key at a time.
#
# REQUIRED: every entry, and every later edit to one, carries a comment directly above it
# recording its provenance:
#   - who made the change, and the date;
#   - where it came from: the commit, PR, or bug report;
#   - the symptom it fixes or the cost it saves, described so a tester can check it;
#   - what it was verified on: the build or commit, the renderer, and the device or GPU.
# A commit message alone is not enough. The 2026-07-22 regeneration of this file dropped
# every comment, and the entries it left could not be traced back to a reason.

# NASCAR Thunder 2002 through NASCAR 06 — count DISPFB blits instead of
# privileged register writes when working out the internal frame rate. Reported
# by yuasasa on Android handhelds.
#
# The register-write detector reads these engines as producing a new image every
# vblank. SkipDuplicateFrames (default on) trusts that answer, so no duplicate is
# ever dropped and every repeated frame still pays for a full present. Counting
# blits into the displayed framebuffer gives the real render rate and lets the
# skip fire again.
#
# Scope is the five titles the reporter named; every serial each was released
# under is listed, since the fix is a property of the engine and not the region.
# The rest of the series is out on his word that it does not need this. NASCAR 07
# is a separate case — upstream GameIndex.yaml already sets the fix on both its
# serials, and restating it here would put its gameFixes list under this overlay's
# clear-then-replace rule, so anything upstream added later would be dropped on
# mobile.
#
# gameFixes only, for that same rule: upstream carries roundSprite on the Thunder
# entries and deinterlace on Total Team Control, and naming gsHWFixes here would
# delete them.
SLES-53635:            # NASCAR 06 - Total Team Control (PAL-E)
  gameFixes:
    - BlitInternalFPSHack
SLUS-20266:            # NASCAR Thunder 2002
  gameFixes:
    - BlitInternalFPSHack
SLUS-20535:            # NASCAR Thunder 2003
  gameFixes:
    - BlitInternalFPSHack
SLUS-20754:            # NASCAR Thunder 2004
  gameFixes:
    - BlitInternalFPSHack
SLUS-20824:            # NASCAR Thunder 2004
  gameFixes:
    - BlitInternalFPSHack
SLUS-21003:            # NASCAR 2005 - Chase for the Cup
  gameFixes:
    - BlitInternalFPSHack
SLUS-21266:            # NASCAR 06 - Total Team Control (NTSC-U)
  gameFixes:
    - BlitInternalFPSHack
SCAJ-20162:
  gsHWFixes:
    autoFlush: 0
    disablePartialInvalidation: 1
    halfPixelOffset: 2
    hwDownloadMode: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
    roundSprite: 1
SCAJ-20169:
  gsHWFixes:
    autoFlush: 1
    coalesceRenderPasses: 1
    halfPixelOffset: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
SCES-54552:
  gsHWFixes:
    autoFlush: 0
    disablePartialInvalidation: 1
    halfPixelOffset: 2
    hwDownloadMode: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
    roundSprite: 1
SCPS-15102:
  gsHWFixes:
    autoFlush: 0
    disablePartialInvalidation: 1
    halfPixelOffset: 2
    hwDownloadMode: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
    roundSprite: 1
SCPS-17013:
  gsHWFixes:
    autoFlush: 0
    disablePartialInvalidation: 1
    halfPixelOffset: 2
    hwDownloadMode: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
    roundSprite: 1
SCPS-19254:
  gsHWFixes:
    autoFlush: 0
    disablePartialInvalidation: 1
    halfPixelOffset: 2
    hwDownloadMode: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
    roundSprite: 1
SCUS-97490:
  gsHWFixes:
    autoFlush: 0
    disablePartialInvalidation: 1
    halfPixelOffset: 2
    hwDownloadMode: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
    roundSprite: 1
SCUS-97572:
  gsHWFixes:
    autoFlush: 0
    disablePartialInvalidation: 1
    halfPixelOffset: 2
    hwDownloadMode: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
    roundSprite: 1
SLES-54185:
  gsHWFixes:
    autoFlush: 1
    coalesceRenderPasses: 1
    halfPixelOffset: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
# Guitar Hero II (PAL) — the note-highway readback is pure cost on a tiler; the
# game never samples the downloaded target back. Also covers GH2 Deluxe, which
# keeps the retail serial.
SLES-54442:
  gsHWFixes:
    hwDownloadMode: 2 # NoReadbacks. Large perf win, no visual regression.
# Guitar Hero III — NoReadbacks. Same rationale as the GH2 entries: the note-highway target is
# never sampled back, so the readback is pure cost on a tiler. gsHWFixes is a clear-then-replace
# map in this overlay, so every upstream key is restated here — dropping one would silently undo
# the crowd-texture / bloom / post-processing fixes.
SLES-54962:
  gsHWFixes:
    autoFlush: 1 # Fixes bloom intensity.
    halfPixelOffset: 4 # Mostly aligns post-processing.
    hwDownloadMode: 2 # NoReadbacks. Large perf win, no visual regression.
    nativeScaling: 1 # Fixes post-processing smoothness and position.
    textureInsideRT: 1 # Crowd textures.
    getSkipCount: "GSC_GuitarHero"
SLES-54974:
  gsHWFixes:
    autoFlush: 1 # Fixes bloom intensity.
    halfPixelOffset: 4 # Mostly aligns post-processing.
    hwDownloadMode: 2 # NoReadbacks. Large perf win, no visual regression.
    nativeScaling: 1 # Fixes post-processing smoothness and position.
    textureInsideRT: 1 # Crowd textures.
    getSkipCount: "GSC_GuitarHero"
# Guitar Hero III (NTSC-K) — see the SLES-549xx note above.
SLKA-25363:
  gsHWFixes:
    autoFlush: 1 # Fixes bloom intensity.
    halfPixelOffset: 4 # Mostly aligns post-processing.
    hwDownloadMode: 2 # NoReadbacks. Large perf win, no visual regression.
    nativeScaling: 1 # Fixes post-processing smoothness and position.
    textureInsideRT: 1 # Crowd textures.
SLKA-25414:
  gsHWFixes:
    autoFlush: 1 # Fixes bloom intensity.
    halfPixelOffset: 4 # Mostly aligns post-processing.
    hwDownloadMode: 2 # NoReadbacks. Large perf win, no visual regression.
    nativeScaling: 1 # Fixes post-processing smoothness and position.
    textureInsideRT: 1 # Crowd textures.
    getSkipCount: "GSC_GuitarHero"
SLPM-66271:
  gsHWFixes:
    autoFlush: 1
    coalesceRenderPasses: 1
    halfPixelOffset: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
SLPM-66629:
  gsHWFixes:
    autoFlush: 1
    coalesceRenderPasses: 1
    halfPixelOffset: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
SLPM-68016:
  gsHWFixes:
    autoFlush: 1
    coalesceRenderPasses: 1
    halfPixelOffset: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
SLUS-20811:
  gsHWFixes:
    drawBuffering: 1
    halfPixelOffset: 2
    hwDownloadMode: 2 # No Readbacks — NFS Underground perf (community request)
    recommendedBlendingLevel: 3
SLUS-21065:
  gsHWFixes:
    drawBuffering: 1
    halfPixelOffset: 5
    hwDownloadMode: 2 # No Readbacks — NFS Underground 2 perf (community request)
    nativeScaling: 1
SLUS-21419:
  gsHWFixes:
    autoFlush: 1
    coalesceRenderPasses: 1
    halfPixelOffset: 5
    nativeScaling: 2
    recommendedBlendingLevel: 4
# Guitar Hero II (NTSC-U) — see the PAL entry above. This is also the serial the
# GH2 Deluxe mod ships under, so Deluxe inherits the fix.
SLUS-21447:
  gsHWFixes:
    hwDownloadMode: 2 # NoReadbacks. Large perf win, no visual regression.
SLUS-21672:
  gsHWFixes:
    autoFlush: 1 # Fixes bloom intensity.
    halfPixelOffset: 4 # Mostly aligns post-processing.
    hwDownloadMode: 2 # NoReadbacks. Large perf win, no visual regression.
    nativeScaling: 1 # Fixes post-processing smoothness and position.
    textureInsideRT: 1 # Crowd textures.
    getSkipCount: "GSC_GuitarHero"
